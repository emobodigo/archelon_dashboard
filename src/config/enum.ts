export enum MessageStatus {
    Success = 'success',
    Error = 'error',
    Warning = 'warning'
}

export enum LabelSize {
    xl = "prime",
    md = "sub",
}

export enum ButtonType {
    GOOD = "good",
    BAD = "bad",
    WARNING = "warning",
    PRIMARY = "primary",
}